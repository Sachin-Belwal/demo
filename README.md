# demo
first repo
<br>
Basic commands for Git-hub
<br>
Status-git status
add- git add . (.for all)
commit- git commit -m "message"
make new repo in github and it's link 
git remote add origin link-
to check remote- git remote -v
to check branch - git branch (master-default branch)
to change branch name - git branch -M main
now finally push to github-git push origin main

<br>Branch
to check branch- git branch
to make new branch - git checkout -b name
to change branch - git checkout name
to delete brach - git brach -d name
to see difference between two brach - git diff name 

to get the merge changes from remote to local - git pull origin main // using git-hub

to merge from command line -git merge main

<br>
if we add changes but not commit - git reset (for all or filname for paticular file)

to reset after commit - git reset HEAD~1
to check all commits - git log
if want to go multiple commits back - git rest hash(can get from git log )
to also delete from the vs code - git reset --hard

<br>fork- to make copy and settings of another repo of something 

<br>
to make new repo in local 
new repo- mkdir nameofdic
to make dict to repo - git init 



